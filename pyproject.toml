[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "swarph-shared"
version = "0.6.1"
description = "Shared substrate primitives for swarph-mesh ecosystem (caller convention, subprocess env scrubbing, JSON-mode harness, peer-name registry, cell.yaml schema). v0.3.2 bundles the adversarial-sweep hardening: subprocess_env billing-redirect scrub, string-aware JSON brace extraction, and peer_registry empty-token/malformed-shape/regex-unification fixes."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [
    { name = "Pierre Samson" },
    { name = "Claude Opus" },
]
keywords = ["swarph", "llm", "mesh", "subprocess", "json-mode"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
# Pure stdlib — no runtime deps
dependencies = []

[project.urls]
Homepage = "https://github.com/BrainSurfing-tech/swarph-shared"
Source = "https://github.com/BrainSurfing-tech/swarph-shared"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# >>> WITHOUT THIS, `pytest` IMPORTS THE **INSTALLED** swarph_shared AND NOT THE
# WORKING TREE — a source edit is invisible to the suite and every test passes
# having exercised the previously-released wheel. MEASURED 2026-08-05 adding
# `vibe` to VALID_PROVIDERS: the contract test failed against site-packages
# 0.5.0 while the tree already had the change, and `PYTHONPATH=src` flipped it.
# A green suite that never loaded your code is the purest false green there is.
pythonpath = ["src"]
addopts = "-v --tb=short"
